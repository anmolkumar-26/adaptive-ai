version: "3"

services:
  backend:
    build: ..
    ports:
      - "8000:8000"

  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: adaptive_ai
    ports:
      - "5432:5432"